#!/bin/sh

cp .tmux.conf ~
cp .inputrc ~

# neovim
curl -Lo /usr/local/bin/nvim https://github.com/neovim/neovim/releases/download/v0.9.5/nvim.appimage
chmod 555 /usr/local/bin/nvim
ln -s .config/nvim ~/.config
